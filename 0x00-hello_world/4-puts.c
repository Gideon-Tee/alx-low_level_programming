#include <stdio.h>

/**
 * main - entry point
 * Return: Return 0 when successful
 */

int main(void)
{
	char text[] = "\"Programming is like building a multilingual puzzle";

	puts(text);
	return (0);
}
